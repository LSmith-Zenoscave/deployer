language: python
python:
- 3.8
- 3.7
- 3.6
install: pip install -U tox-travis coverage codecov
script:
- tox
- coverage run --source deployer -m pytest
after_success:
- codecov
deploy:
  provider: pypi
  distributions: sdist bdist_wheel
  user: lsmith_zeno
  password: ${PYPI_PASSWORD}
  on:
    tags: true
    repo: LSmith-Zenoscave/deployer
    python: 3.8
